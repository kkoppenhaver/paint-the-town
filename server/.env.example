# Copy to server/.env (or export) to enable live Google Routes travel.
# Without a key the server uses the offline `estimate` travel provider.
GOOGLE_MAPS_API_KEY=
# Port for the room server (default 8787). The client expects ws://localhost:8787.
PORT=8787
